{
  extends: ["../base/gitattributes.json5"],
  rules: [
    {
      files: [".gitattributes"],
      includeLines: [
        // Does not include poetry lock file lines in diffs
        "poetry.lock -diff",
      ],
    },
  ],
}
