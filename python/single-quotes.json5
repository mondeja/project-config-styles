{
  extends: [
    "pre-commit/double-quote-string-fixer.json5",
    "pre-commit/autopep8.json5",
  ],
  rules: [
    {
      files: ["pyproject.toml"],
      JMESPathsMatch: [
        ["contains(keys(@), 'tool')", true],
        ["contains(keys(tool), 'flakeheaven')", true],
        ["tool.flakeheaven.inline_quotes", "single"],
      ],
    },
  ],
}
