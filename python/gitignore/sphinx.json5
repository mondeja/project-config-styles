{
  rules: [
    {
      files: [".gitignore"],
      JMESPathsMatch: [
        [
          "contains(@, 'docs/_build/')",
          true,
          "insert(@, `-1`, 'docs/_build/')",
        ],
      ],
    },
  ],
}
