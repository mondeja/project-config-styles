{
  rules: [
    {
      files: [".pre-commit-config.yaml"],
      hint: "Pre-commit hooks must not execute shell scripts use the basic Shell. Rewrite your script yo make them POSIX-compatible and execute them using 'sh -c' in pre-commit hook entries",
      JMESPathsMatch: [
        ["length(repos[*].hooks[*].entry[] | [?starts_with(@, `bash`)])", 0],
        ["length(repos[*].hooks[*].entry[] | [?starts_with(@, `zsh`)])", 0],
        ["length(repos[*].hooks[*].entry[] | [?starts_with(@, `ksh`)])", 0],
        ["length(repos[*].hooks[*].entry[] | [?starts_with(@, `tcsh`)])", 0],
      ],
    },
  ],
}
