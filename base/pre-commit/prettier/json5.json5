{
  rules: [
    {
      // Use prettier to format JSON5 files
      files: [".pre-commit-config.yaml"],
      JMESPathsMatch: [
        [
          "contains(repos[?repo=='https://github.com/pre-commit/mirrors-prettier'] | [0].hooks[0].types_or, 'json5')",
          true,
        ],
      ],
    },
  ],
}
